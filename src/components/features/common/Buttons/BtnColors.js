
const facebookDefaultColor = "#4267B2"
const facebookTouchedColor = "#183b80"
const instagramDefaultColor = "#C13584"
const instagramTouchedColor = "#991861"

const defaultColors = {
  facebook: facebookDefaultColor,
  instagram: instagramDefaultColor,
}

const touchedColors = {
  facebook: facebookTouchedColor,
  instagram: instagramTouchedColor,
}

const colors = {
  default: defaultColors,
  touched: touchedColors,
}

export default colors
